<UserControl x:Class="TestSPWrite.UCBytes"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TestSPWrite"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition Width="auto"/>
        </Grid.ColumnDefinitions>
        <TextBox Name="txt" Text="{Binding Path=BytesText, Mode=OneWay}"
                 VerticalAlignment="Center" FontSize="20"/>
        <Button Name="btn" Click="btn_Click" Margin="5" Grid.Column="1" HorizontalAlignment="Center" VerticalAlignment="Center" MinWidth="50">发送</Button>
    </Grid>
</UserControl>
